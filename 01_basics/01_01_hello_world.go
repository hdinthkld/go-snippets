// The most basic working go program
package main // All directly running programs must be part of the 'main' package

import "fmt" // Used to gain access to external features and libraries

func main() {
	// The place where the program starts if runnable directly
	fmt.Println("Hello World") // Prints out at friendly message to the screen
}
